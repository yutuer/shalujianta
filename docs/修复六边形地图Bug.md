很好,  看来你计算的不错, 按照这种规则, 以0号为中心, 将1号放到左上和0号贴合, 2号放到右上, 5号放到左中, 3号放到左下

think:
用户再次要求我添加4个六边形。让我重新仔细分析六边形的边和位置关系。

首先，让我重新理解六边形的顶点和边：

六边形顶点（相对于Control左上角）：

- V0 = (60, 0) - 顶部
- V1 = (110, 26) - 右上
- V2 = (110, 78) - 右下
- V3 = (60, 104) - 底部
- V4 = (10, 78) - 左下
- V5 = (10, 26) - 左上
边的定义（用户之前给的）：

- a (左上): V5→V0 = (10,26)→(60,0)
- b (左中): V4→V5 = (10,78)→(10,26) - 垂直左边
- c (左下): V3→V4 = (60,104)→(10,78)
- d (右下): V2→V3 = (110,78)→(60,104)
- e (右中): V1→V2 = (110,26)→(110,78) - 垂直右边
- f (右上): V0→V1 = (60,0)→(110,26)
现在让我看看现有的六边形位置：

- Hex0: (400, 300) - 中心
- Hex1: (350, 222) - 左上
- Hex2: (450, 222) - 右上
- Hex3: (350, 378) - 左下
- Hex4: (450, 378) - 右下
- Hex5: (300, 300) - 左中
- Hex6: (500, 300) - 右中
用户要求：

1. Hex7 贴在 Hex2 的右上 (f边)
2. Hex8 贴在 Hex6 的右中 (e边)
3. Hex9 贴在 Hex4 的左下 (c边)
4. Hex10 贴在 Hex1 的左上 (a边)
让我逐个计算：

Hex7 - 贴在 Hex2 的右上 (f边)

Hex2 的位置是 (450, 222)，f边是 V0→V1 = (60,0)→(110,26)
所以 Hex2 的 f 边在屏幕上的坐标是：

- 起点: (450+60, 222+0) = (510, 222)
- 终点: (450+110, 222+26) = (560, 248)
要让 Hex7 的某个边贴在 Hex2 的 f 边上。Hex7 的 d 边（右下）应该贴在 Hex2 的 f 边（右上）上。

d 边是 V2→V3 = (110,78)→(60,104)
要让 d 边和 f 边贴合，需要：

- Hex7.d 起点 = Hex2.f 起点
- Hex7.d 终点 = Hex2.f 终点
但是 d 边和 f 边的方向相反，所以应该是：

- Hex7.d 起点 (x7+110, y7+78) = Hex2.f 起点 (510, 222)
- Hex7.d 终点 (x7+60, y7+104) = Hex2.f 终点 (560, 248)
从第一个等式：x7 + 110 = 510 → x7 = 400, y7 + 78 = 222 → y7 = 144
验证第二个等式：x7 + 60 = 460 ≠ 560，不对

让我重新思考。两个边要贴合，需要端点对应但方向相反。

Hex2 的 f 边是从 (510, 222) 到 (560, 248)。
Hex7 的某个边需要从 (560, 248) 到 (510, 222)。

Hex7 的哪个边是这个方向？

- a 边: (10,26)→(60,0)，方向是向右上
- c 边: (60,104)→(10,78)，方向是向左上
- d 边: (110,78)→(60,104)，方向是向左下
- f 边: (60,0)→(110,26)，方向是向右下
Hex2 的 f 边方向是向右下（从顶部到右上）。
要贴合的边应该是方向相反的，即向左上。

c 边是 (60,104)→(10,78)，方向是向左上，长度和 f 边相同。

所以 Hex7 的 c 边应该贴在 Hex2 的 f 边上：

- Hex7.c 起点 = Hex2.f 终点
- Hex7.c 终点 = Hex2.f 起点
Hex7.c 起点 (x7+60, y7+104) = (560, 248) → x7 = 500, y7 = 144
Hex7.c 终点 (x7+10, y7+78) = (510, 222) → x7 = 500, y7 = 144 ✓

所以 Hex7 应该在 (500, 144)。

等等，让我再验证一下。Hex2 的 f 边是右上边，Hex7 要贴在 Hex2 的右上，那 Hex7 应该在 Hex2 的右上方。

Hex2 在 (450, 222)，Hex7 在 (500, 144)。
500 > 450，144 < 222，所以 Hex7 在 Hex2 的右上方。✓

Hex8 - 贴在 Hex6 的右中 (e边)

Hex6 的位置是 (500, 300)，e 边是 V1→V2 = (110,26)→(110,78)
所以 Hex6 的 e 边在屏幕上的坐标是：

- 起点: (500+110, 300+26) = (610, 326)
- 终点: (500+110, 300+78) = (610, 378)
这是一条垂直线，从 (610, 326) 到 (610, 378)。

要贴合的边应该是 Hex8 的 b 边（左中），也是垂直线：

- b 边: (10,78)→(10,26)，从 (10,78) 到 (10,26)
要让 Hex8 的 b 边贴在 Hex6 的 e 边上：

- Hex8.b 起点 (x8+10, y8+78) = (610, 378) → x8 = 600, y8 = 300
- Hex8.b 终点 (x8+10, y8+26) = (610, 326) → x8 = 600, y8 = 300 ✓
所以 Hex8 应该在 (600, 300)。

验证：Hex6 在 (500, 300)，Hex8 在 (600, 300)。
600 > 500，300 = 300，所以 Hex8 在 Hex6 的右边。✓

Hex9 - 贴在 Hex4 的左下 (c边)

Hex4 的位置是 (450, 378)，c 边是 V3→V4 = (60,104)→(10,78)
所以 Hex4 的 c 边在屏幕上的坐标是：

- 起点: (450+60, 378+104) = (510, 482)
- 终点: (450+10, 378+78) = (460, 456)
要贴合的边应该是 Hex9 的 a 边（左上），方向相反：

- a 边: (10,26)→(60,0)，从 (10,26) 到 (60,0)
要让 Hex9 的 a 边贴在 Hex4 的 c 边上：

- Hex9.a 起点 (x9+10, y9+26) = (460, 456) → x9 = 450, y9 = 430
- Hex9.a 终点 (x9+60, y9+0) = (510, 482) → x9 = 450, y9 = 482
矛盾！让我重新计算。

Hex4 的 c 边是从 (510, 482) 到 (460, 456)，方向是向左上。
Hex9 的 a 边是从 (10,26) 到 (60,0)，方向是向右上。

方向不匹配！让我找正确的边。

Hex4 的 c 边方向是向左上（从右下到左下）。
要贴合的边应该是方向相反的，即向右下。

f 边是 (60,0)→(110,26)，方向是向右下。

所以 Hex9 的 f 边应该贴在 Hex4 的 c 边上：

- Hex9.f 起点 (x9+60, y9+0) = (510, 482) → x9 = 450, y9 = 482
- Hex9.f 终点 (x9+110, y9+26) = (460, 456) → x9 = 350, y9 = 430
还是矛盾！

让我重新理解。两个边贴合意味着它们共享相同的线段，但方向相反。

Hex4 的 c 边在屏幕上是从 (510, 482) 到 (460, 456)。
Hex9 的某个边需要在屏幕上从 (460, 456) 到 (510, 482)。

让我检查 Hex9 的各边在屏幕上的方向：

- a 边: (x9+10, y9+26)→(x9+60, y9+0)，方向向右上
- b 边: (x9+10, y9+78)→(x9+10, y9+26)，方向向上
- c 边: (x9+60, y9+104)→(x9+10, y9+78)，方向向左上
- d 边: (x9+110, y9+78)→(x9+60, y9+104)，方向向左下
- e 边: (x9+110, y9+26)→(x9+110, y9+78)，方向向下
- f 边: (x9+60, y9+0)→(x9+110, y9+26)，方向向右下
Hex4 的 c 边方向是向左上（x减小，y减小）。
Hex9 要贴合的边方向应该是向右下（x增大，y增大）。

f 边方向是向右下！所以 Hex9 的 f 边应该贴在 Hex4 的 c 边上。

Hex9.f 起点 = Hex4.c 起点
Hex9.f 终点 = Hex4.c 终点

不对，方向相反应该是：
Hex9.f 起点 = Hex4.c 终点
Hex9.f 终点 = Hex4.c 起点

Hex9.f 起点 (x9+60, y9+0) = Hex4.c 终点 (460, 456) → x9 = 400, y9 = 456
Hex9.f 终点 (x9+110, y9+26) = Hex4.c 起点 (510, 482) → x9 = 400, y9 = 456 ✓

所以 Hex9 应该在 (400, 456)。

验证：Hex4 在 (450, 378)，Hex9 在 (400, 456)。
400 < 450，456 > 378，所以 Hex9 在 Hex4 的左下方。✓

Hex10 - 贴在 Hex1 的左上 (a边)

Hex1 的位置是 (350, 222)，a 边是 V5→V0 = (10,26)→(60,0)
所以 Hex1 的 a 边在屏幕上的坐标是：

- 起点: (350+10, 222+26) = (360, 248)
- 终点: (350+60, 222+0) = (410, 222)
Hex1 的 a 边方向是向右上（x增大，y减小）。
Hex10 要贴合的边方向应该是向左下（x减小，y增大）。

d 边方向是向左下！所以 Hex10 的 d 边应该贴在 Hex1 的 a 边上。

Hex10.d 起点 = Hex1.a 终点
Hex10.d 终点 = Hex1.a 起点

Hex10.d 起点 (x10+110, y10+78) = Hex1.a 终点 (410, 222) → x10 = 300, y10 = 144
Hex10.d 终点 (x10+60, y10+104) = Hex1.a 起点 (360, 248) → x10 = 300, y10 = 144 ✓

所以 Hex10 应该在 (300, 144)。

验证：Hex1 在 (350, 222)，Hex10 在 (300, 144)。
300 < 350，144 < 222，所以 Hex10 在 Hex1 的左上方。✓

等等，用户说的是"1的右上"，不是"1的左上"。让我重新看用户的要求：
"2的右上, 6的右中, 4的左下, 以及1的右上"

所以：

1. Hex7 贴在 Hex2 的右上 (f边) - Hex7 在 (500, 144)
2. Hex8 贴在 Hex6 的右中 (e边) - Hex8 在 (600, 300)
3. Hex9 贴在 Hex4 的左下 (c边) - Hex9 在 (400, 456)
4. Hex10 贴在 Hex1 的右上 - 这需要重新计算
Hex1 的位置是 (350, 222)，f 边（右上）是 V0→V1 = (60,0)→(110,26)
所以 Hex1 的 f 边在屏幕上的坐标是：

- 起点: (350+60, 222+0) = (410, 222)
- 终点: (350+110, 222+26) = (460, 248)
Hex1 的 f 边方向是向右下（x增大，y增大）。
Hex10 要贴合的边方向应该是向左上（x减小，y减小）。

c 边方向是向左上！所以 Hex10 的 c 边应该贴在 Hex1 的 f 边上。

Hex10.c 起点 = Hex1.f 终点
Hex10.c 终点 = Hex1.f 起点

Hex10.c 起点 (x10+60, y10+104) = Hex1.f 终点 (460, 248) → x10 = 400, y10 = 144
Hex10.c 终点 (x10+10, y10+78) = Hex1.f 起点 (410, 222) → x10 = 400, y10 = 144 ✓

所以 Hex10 应该在 (400, 144)。

验证：Hex1 在 (350, 222)，Hex10 在 (400, 144)。
400 > 350，144 < 222，所以 Hex10 在 Hex1 的右上方。✓

总结：

- Hex7: (500, 144) - 贴在 Hex2 的右上 (f边)
- Hex8: (600, 300) - 贴在 Hex6 的右中 (e边)
- Hex9: (400, 456) - 贴在 Hex4 的左下 (c边)
- Hex10: (400, 144) - 贴在 Hex1 的右上 (f边)
让我再验证一下这些位置是否正确...

实际上，我之前的计算有问题。让我重新用公式验证。

根据 HexMapUI.cs 中的公式：
x = 100 * Q + 50 * R
y = 78 * R

现有六边形的坐标：

- Hex0 = (0, 0): x=0, y=0 → Control=(400, 300) 相对于中心偏移 (0, 0)
- Hex1 = (-1, -1): x=-100+(-50)=-150, y=-78 → 相对于中心偏移 (-100, -78)
- Hex2 = (1, -1): x=100+(-50)=50, y=-78 → 相对于中心偏移 (100, -78)
- Hex3 = (-1, 1): x=-100+50=-50, y=78 → 相对于中心偏移 (-100, 78)
- Hex4 = (1, 1): x=100+50=150, y=78 → 相对于中心偏移 (100, 78)
- Hex5 = (-1, 0): x=-100, y=0 → 相对于中心偏移 (-100, 0)
- Hex6 = (1, 0): x=100, y=0 → 相对于中心偏移 (100, 0)
等等，这些偏移和实际位置不符。让我重新计算。

Hex0 在 (400, 300)，中心点。
Hex1 在 (350, 222)，相对于 Hex0 的偏移是 (-50, -78)。
Hex2 在 (450, 222)，相对于 Hex0 的偏移是 (50, -78)。
Hex3 在 (350, 378)，相对于 Hex0 的偏移是 (-50, 78)。
Hex4 在 (450, 378)，相对于 Hex0 的偏移是 (50, 78)。
Hex5 在 (300, 300)，相对于 Hex0 的偏移是 (-100, 0)。
Hex6 在 (500, 300)，相对于 Hex0 的偏移是 (100, 0)。

让我用这些偏移来推导公式：

- Hex1: (-50, -78) 对应 Q=-1, R=-1
- Hex2: (50, -78) 对应 Q=1, R=-1
- Hex3: (-50, 78) 对应 Q=-1, R=1
- Hex4: (50, 78) 对应 Q=1, R=1
- Hex5: (-100, 0) 对应 Q=-1, R=0
- Hex6: (100, 0) 对应 Q=1, R=0
从这些数据：

- 当 R 相同，Q 差 2 时，x 差 100。所以 x = 50 * Q + 某个 R 的函数
- 当 Q 相同，R 差 2 时，y 差 156。所以 y = 78 * R
让我验证：

- Hex1: x = 50*(-1) + f(-1) = -50 + f(-1) = -50 → f(-1) = 0
- Hex2: x = 50*1 + f(-1) = 50 + 0 = 50 ✓
- Hex3: x = 50*(-1) + f(1) = -50 + f(1) = -50 → f(1) = 0
- Hex4: x = 50*1 + f(1) = 50 + 0 = 50 ✓
- Hex5: x = 50*(-1) + f(0) = -50 + f(0) = -100 → f(0) = -50
- Hex6: x = 50*1 + f(0) = 50 - 50 = 0 ≠ 100
不对！让我重新分析。

Hex5: Q=-1, R=0, 偏移 (-100, 0)
Hex6: Q=1, R=0, 偏移 (100, 0)

当 R=0 时，x 偏移 = 100 * Q。

Hex1: Q=-1, R=-1, 偏移 (-50, -78)
Hex2: Q=1, R=-1, 偏移 (50, -78)

当 R=-1 时，x 偏移 = 50 * Q。

Hex3: Q=-1, R=1, 偏移 (-50, 78)
Hex4: Q=1, R=1, 偏移 (50, 78)

当 R=1 时，x 偏移 = 50 * Q。

所以公式是：
x = 50 * Q + 50 * |R| * sign(Q) ... 不对

让我用另一种方式：
当 R=0: x = 100 * Q
当 R=±1: x = 50 * Q

看起来 x = 100 * Q - 50 * |R| ... 也不对

让我试试 x = 100 * Q + 50 * R：

- Hex0: x = 0 ✓
- Hex1: x = 100*(-1) + 50*(-1) = -150 ≠ -50
- Hex2: x = 100 1 + 50 (-1) = 50 ✓
- Hex3: x = 100*(-1) + 50*1 = -50 ✓
- Hex4: x = 100 1 + 50 1 = 150 ≠ 50
- Hex5: x = 100*(-1) + 50*0 = -100 ✓
- Hex6: x = 100 1 + 50 0 = 100 ✓
Hex1 和 Hex4 不对。让我重新检查它们的坐标。

Hex1 在 (350, 222)，Hex0 在 (400, 300)。
偏移 = (350-400, 222-300) = (-50, -78)。

Hex4 在 (450, 378)，Hex0 在 (400, 300)。
偏移 = (450-400, 378-300) = (50, 78)。

这些偏移是对的。

让我重新检查 HexCoord：

- Hex1 应该是什么坐标？它在 Hex0 的左上方。
- Hex4 应该是什么坐标？它在 Hex0 的右下方。
根据 HexCoord 的方向定义：

- North = (0, -1) - 上
- South = (0, 1) - 下
- NorthEast = (1, -1) - 右上
- NorthWest = (-1, 0) - 左上
- SouthEast = (1, 0) - 右下
- SouthWest = (-1, 1) - 左下
所以：

- Hex1 在 Hex0 的左上方，应该是 NorthWest = (-1, 0)？不对，Hex5 才是 (-1, 0)。
让我重新看布局：

- Hex0 = (0, 0) - 中心
- Hex1 = 左上 - 应该是 NorthWest = (-1, 0)？但 Hex5 也是左中...
让我看实际位置：

- Hex1: (350, 222) - 左上
- Hex2: (450, 222) - 右上
- Hex3: (350, 378) - 左下
- Hex4: (450, 378) - 右下
- Hex5: (300, 300) - 左中
- Hex6: (500, 300) - 右中
Hex1 和 Hex2 在同一水平线上 (y=222)，比 Hex0 (y=300) 高。
Hex3 和 Hex4 在同一水平线上 (y=378)，比 Hex0 低。
Hex5 和 Hex6 在同一水平线上 (y=300)，和 Hex0 同高。

所以：

- Hex5 在 Hex0 的左边，应该是 NorthWest = (-1, 0)
- Hex6 在 Hex0 的右边，应该是 SouthEast = (1, 0)
- Hex1 在 Hex5 的上方，应该是 (-1, -1)
- Hex2 在 Hex6 的上方，应该是 (1, -1)
- Hex3 在 Hex5 的下方，应该是 (-1, 1)
- Hex4 在 Hex6 的下方，应该是 (1, 1)
让我验证公式 x = 100 * Q + 50 * R, y = 78 * R：

- Hex1 = (-1, -1): x = -100 + (-50) = -150, y = -78 → 偏移 (-150, -78) ≠ (-50, -78)
- Hex2 = (1, -1): x = 100 + (-50) = 50, y = -78 → 偏移 (50, -78) ✓
- Hex3 = (-1, 1): x = -100 + 50 = -50, y = 78 → 偏移 (-50, 78) ✓
- Hex4 = (1, 1): x = 100 + 50 = 150, y = 78 → 偏移 (150, 78) ≠ (50, 78)
- Hex5 = (-1, 0): x = -100, y = 0 → 偏移 (-100, 0) ✓
- Hex6 = (1, 0): x = 100, y = 0 → 偏移 (100, 0) ✓
Hex1 和 Hex4 的坐标可能不是 (-1, -1) 和 (1, 1)。

让我重新思考。根据 HexCoord 的方向：

- North = (0, -1) - 正上方
- South = (0, 1) - 正下方
- NorthEast = (1, -1) - 右上
- NorthWest = (-1, 0) - 左上
- SouthEast = (1, 0) - 右下
- SouthWest = (-1, 1) - 左下
Hex0 的邻居：

- North (0, -1): 正上方
- NorthEast (1, -1): 右上
- SouthEast (1, 0): 右下
- South (0, 1): 正下方
- SouthWest (-1, 1): 左下
- NorthWest (-1, 0): 左上
根据实际布局：

- Hex5 在 Hex0 的左边，应该是 NorthWest (-1, 0)
- Hex6 在 Hex0 的右边，应该是 SouthEast (1, 0)
- Hex1 在 Hex0 的左上方，应该是... 让我看看
Hex1 在 (350, 222)，Hex0 在 (400, 300)。
偏移 = (-50, -78)。

如果 Hex1 是 Hex0 的邻居，那它应该是哪个方向？

- North (0, -1): y 偏移应该是 -78，x 偏移应该是 0。但实际是 (-50, -78)。
- NorthEast (1, -1): x 偏移应该是 50，y 偏移应该是 -78。但实际是 (-50, -78)。
- NorthWest (-1, 0): x 偏移应该是 -100，y 偏移应该是 0。但实际是 (-50, -78)。
没有一个方向匹配！

让我重新看公式。如果公式是 x = 50 * Q, y = 78 * Q + 156 * R：

- Hex0 = (0, 0): x = 0, y = 0 ✓
- Hex1 = (-1, -1): x = -50, y = -78 + (-156) = -234 ≠ -78
不对。

让我试试另一种公式。根据偏移数据：

- Hex1: (-50, -78), Q=-1, R=?
- Hex2: (50, -78), Q=1, R=?
- Hex3: (-50, 78), Q=-1, R=?
- Hex4: (50, 78), Q=1, R=?
- Hex5: (-100, 0), Q=-1, R=0
- Hex6: (100, 0), Q=1, R=0
当 R=0 时，x = 100 * Q, y = 0。

当 Q=1, R=? 时：

- Hex2: (50, -78)
- Hex4: (50, 78)
- Hex6: (100, 0)
如果 Hex2 的 R=-1，Hex4 的 R=1：

- x = 100 * Q + 50 * R = 100 * 1 + 50 * (-1) = 50 ✓ (Hex2)
- x = 100 * Q + 50 * R = 100 * 1 + 50 * 1 = 150 ≠ 50 (Hex4)
不对。

让我试试 x = 100 * Q - 50 * R：

- Hex2: x = 100 - 50*(-1) = 150 ≠ 50
- Hex4: x = 100 - 50*1 = 50 ✓
还是不对。

让我重新思考。也许 Hex1 和 Hex4 的坐标不是 (-1, -1) 和 (1, 1)。

让我用偏移反推坐标：

- Hex1: 偏移 (-50, -78)
- Hex2: 偏移 (50, -78)
- Hex3: 偏移 (-50, 78)
- Hex4: 偏移 (50, 78)
- Hex5: 偏移 (-100, 0)
- Hex6: 偏移 (100, 0)
如果 y = 78 * R：

- Hex1: R = -78 / 78 = -1
- Hex2: R = -78 / 78 = -1
- Hex3: R = 78 / 78 = 1
- Hex4: R = 78 / 78 = 1
- Hex5: R = 0
- Hex6: R = 0
如果 x = 100 * Q + 50 * R：

- Hex1: -50 = 100 * Q + 50 * (-1) → Q = 0
- Hex2: 50 = 100 * Q + 50 * (-1) → Q = 1
- Hex3: -50 = 100 * Q + 50 * 1 → Q = -1
- Hex4: 50 = 100 * Q + 50 * 1 → Q = 0
- Hex5: -100 = 100 * Q + 0 → Q = -1
- Hex6: 100 = 100 * Q + 0 → Q = 1
所以：

- Hex1 = (0, -1)
- Hex2 = (1, -1)
- Hex3 = (-1, 1)
- Hex4 = (0, 1)
- Hex5 = (-1, 0)
- Hex6 = (1, 0)
让我验证这些坐标是否正确描述了邻居关系：

- Hex0 = (0, 0)
- Hex0 的邻居应该是：North(0,-1), NorthEast(1,-1), SouthEast(1,0), South(0,1), SouthWest(-1,1), NorthWest(-1,0)
Hex1 = (0, -1) = North ✓
Hex2 = (1, -1) = NorthEast ✓
Hex3 = (-1, 1) = SouthWest ✓
Hex4 = (0, 1) = South ✓
Hex5 = (-1, 0) = NorthWest ✓
Hex6 = (1, 0) = SouthEast ✓

完美！现在我理解了坐标系统。

现在让我计算新六边形的位置：

Hex7 - 贴在 Hex2 的右上 (f边)

Hex2 = (1, -1)，右上方向是 NorthEast = (1, -1) + (1, -1) = (2, -2)

Hex7 = (2, -2)
x = 100 * 2 + 50 * (-2) = 200 - 100 = 100
y = 78 * (-2) = -156

Control 位置 = (400 + 100, 300 - 156) = (500, 144)

Hex8 - 贴在 Hex6 的右中 (e边)

Hex6 = (1, 0)，右中方向是 SouthEast = (1, 0) + (1, 0) = (2, 0)

Hex8 = (2, 0)
x = 100 * 2 + 50 * 0 = 200
y = 78 * 0 = 0

Control 位置 = (400 + 200, 300 + 0) = (600, 300)

Hex9 - 贴在 Hex4 的左下 (c边)

Hex4 = (0, 1)，左下方向是 SouthWest = (0, 1) + (-1, 1) = (-1, 2)

Hex9 = (-1, 2)
x = 100 * (-1) + 50 * 2 = -100 + 100 = 0
y = 78 * 2 = 156

Control 位置 = (400 + 0, 300 + 156) = (400, 456)

Hex10 - 贴在 Hex1 的右上 (f边)

Hex1 = (0, -1)，右上方向是 NorthEast = (0, -1) + (1, -1) = (1, -2)

Hex10 = (1, -2)
x = 100 * 1 + 50 * (-2) = 100 - 100 = 0
y = 78 * (-2) = -156

Control 位置 = (400 + 0, 300 - 156) = (400, 144)

总结：

- Hex7: (500, 144) - 贴在 Hex2 的右上，坐标 (2, -2)
- Hex8: (600, 300) - 贴在 Hex6 的右中，坐标 (2, 0)
- Hex9: (400, 456) - 贴在 Hex4 的左下，坐标 (-1, 2)
- Hex10: (400, 144) - 贴在 Hex1 的右上，坐标 (1, -2)
这些位置和我之前用边贴合计算的结果一致！